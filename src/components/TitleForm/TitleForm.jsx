import React from 'react';

// eslint-disable-next-line react/prop-types
function TitleForm({ text }) {
  return (
    <h1 className="text-center my-5 text-2xl">{text}</h1>
  );
}

export default TitleForm;
